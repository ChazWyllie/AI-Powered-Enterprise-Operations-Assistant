
# WP8 — Frontend UI Skeleton (Website MVP)

## Objective
Create a web UI that calls the FastAPI `/chat` endpoint and renders results cleanly.

## Confirm/Decide
- [ ] Hosting: Vercel? (recommended)
- [ ] Pages: `/` chat, `/about`, `/demo`, `/security` (yes/no)

## Planner Outputs
- Page map + component breakdown
- Response → UI mapping (answer/plan/actions/script/audit)

## Tests First
- UI: send request triggers fetch
- UI: mode toggle affects payload
- UI: renderer handles missing fields gracefully

## Acceptance Criteria
- Working local UI: prompt input + mode toggle + response render
